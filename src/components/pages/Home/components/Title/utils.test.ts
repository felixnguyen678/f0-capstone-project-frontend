describe('utils.ts', () => {
  it('test unit test for utils methods', () => {
    const mockValue = 1
    const expectedValue = 1

    expect(mockValue).toEqual(expectedValue)
  })
})

export default {}
